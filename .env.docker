# Base de Datos MySQL
MYSQL_ROOT_PASSWORD=rootpassword_change_me
MYSQL_DATABASE=hmp_db
MYSQL_USER=hmp_user
MYSQL_PASSWORD=hmp_password_change_me

# Backend
JWT_SECRET=tu_clave_secreta_muy_larga_y_segura_cambiar_en_produccion
JWT_EXPIRES=7d
CORS_ORIGIN=http://localhost

# AWS S3 (opcional, solo si usas S3)
AWS_REGION=us-east-1
AWS_S3_BUCKET=hmp-vitam-bucket
AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_key

# Frontend
VITE_API_URL=http://localhost:8080/api

